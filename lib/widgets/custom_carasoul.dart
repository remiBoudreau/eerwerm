import 'package:flutter/material.dart';

class Recommend extends StatelessWidget {
  const Recommend({super.key});

  @override
  Widget build(BuildContext context) {
    return (Placeholder(
      fallbackHeight: 100,
      fallbackWidth: 100,
    ));
  }
}

// Carasoul
// Have a Header Over, limit 8 items, plus see more button 